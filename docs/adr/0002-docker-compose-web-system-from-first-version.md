# Docker Compose web system from the first version

The first version will be a Docker Compose deployable web system rather than a CLI-only prototype. This keeps the data source adapters, prediction engine, API, frontend, database, AI analysis report generation, and backtest workflow close to the intended production shape while still allowing thin vertical slices.
