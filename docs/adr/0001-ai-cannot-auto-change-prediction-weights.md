# AI cannot automatically change prediction weights

DeepSeek and GPT may generate weight recommendations, but the prediction engine will only use reviewed, versioned, and backtested weights. This keeps match predictions reproducible, auditable, and protected from overfitting or transient news noise.
